pre-commit:
  parallel: true
  jobs:
    - name: lint
      run: pnpm lint {staged_files}
      stage_fixed: true

    - name: format
      run: pnpm format --no-error-on-unmatched-pattern {staged_files}
      stage_fixed: true
